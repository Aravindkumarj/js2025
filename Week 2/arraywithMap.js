/*Create an array of numbers: [2, 4, 6, 8, 10]

Use .map() to create a new array where each number is tripled.

Log the new array to the console. */

let numbers = [2, 4, 6, 8, 10]
let tripled = numbers.map(number => number * 3)
console.log(tripled)